//
// Generated by Microsoft (R) HLSL Shader Compiler 9.29.952.3111
//
//   fxc /nologo /T ps_2_a /E PS /Fc
//    D:\game-development\workspace\active\gameversity-advanced-rendeering-class\final-project-character-engine\shaders\diffuseSpecular.psh
//    D:\game-development\workspace\active\gameversity-advanced-rendeering-class\final-project-character-engine\shaders\diffuseSpecular.fxp
//
//
// Parameters:
//
//   sampler2D BumpMapSampler;
//   sampler2D ColorMapSampler;
//   sampler2D ShadowMapSampler;
//   bool bHDR;
//
//
// Registers:
//
//   Name             Reg   Size
//   ---------------- ----- ----
//   bHDR             c29      1
//   ColorMapSampler  s0       1
//   BumpMapSampler   s1       1
//   ShadowMapSampler s2       1
//

    ps_2_x
    def c0, -0.5, 4, 25, 0.200000003
    def c1, 1, 0, 17.672987, 64
    def c2, 0.00390625, 256, -64, 0.0565835275
    dcl t0.xy
    dcl t1.xyz
    dcl t2.xyz
    dcl t3
    dcl t4.x
    dcl_2d s0
    dcl_2d s1
    dcl_2d s2
    texld r0, t0, s0
    texld r1, t0, s1
    add r1.xyz, r1, c0.x
    add r1.xyz, r1, r1
    nrm r2.xyz, t2
    dp3_sat r1.w, r1, r2
    add r2.w, r1.w, r1.w
    mad r1.xyz, r2.w, r1, -r2
    nrm r2.xyz, r1
    nrm r1.xyz, t1
    dp3_sat r1.x, r2, r1
    pow r2.x, r1.x, c0.z
    min r1.x, r0.w, r2.x
    mad r2, r0, r1.w, r1.x
    mul_sat r1.x, r1.w, c0.y
    mul r1, r2, r1.x
    mad r0, r0, c0.w, r1
    texldp r1, t3, s2
    add r1.x, -r1.x, t4.x
    cmp r1.x, r1.x, c1.x, c1.y
    mul r0, r0, r1.x
    max r1.x, r0.x, r0.y
    max r2.x, r1.x, r0.z
    log r1.x, r2.x
    mul r1.x, r1.x, c1.z
    frc r1.y, r1.x
    add r1.x, -r1.y, r1.x
    add r1.x, r1.x, c1.w
    mul_sat r1.w, r1.x, c2.x
    mad r2.x, r1.w, c2.y, c2.z
    mul r2.x, r2.x, c2.w
    exp r2.x, r2.x
    rcp r2.x, r2.x
    mul r1.xyz, r0, r2.x
    cmp r0, -c29.x, r0, r1
    mov oC0, r0

// approximately 44 instruction slots used (3 texture, 41 arithmetic)
